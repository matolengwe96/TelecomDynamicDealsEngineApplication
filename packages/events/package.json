{
  "name": "@dde/events",
  "version": "1.0.0",
  "private": true,
  "description": "Shared event schemas for async inter-service communication",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "uuid": "^11.0.5"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
